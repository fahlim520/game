# Assets

The prototype renders the game with Pygame geometric shapes, so no external
image files are required. This keeps the repository easy to run in a classroom
or competition environment.

Replace the drawing helpers in `main.py` with sprites when production art is
available.
